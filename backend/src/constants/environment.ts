/**
 * Environment Value Constants
 * Standard environment values used throughout the application
 */
export const ENVIRONMENT = {
  DEVELOPMENT: "development",
  PRODUCTION: "production",
  TEST: "test",
  TRUE: "true",
} as const;
