/**
 * Our own API endpoint routes
 */
export const API_ROUTES = {
  AUTH: "/auth",
  AUTH_GUEST: "/auth/guest",
  DOCS: "/docs",
  SIGMET: "/sigmet",
  AIRSIGMET: "/airsigmet",
  HEALTH: "/health",
} as const;
